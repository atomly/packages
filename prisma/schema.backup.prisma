generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://postgres:password@localhost:5432/test"
}

model users {
  age       Int
  email     String @unique
  firstName String
  id        Int    @id
  lastName  String
  password  String
}