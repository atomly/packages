generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("POSTGRESQL_URL")
}

model posts {
  body   String
  header String
  id     Int    @id
}

model users {
  email    String @unique
  id       Int    @id
  password String
}