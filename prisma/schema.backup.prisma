generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("POSTGRESQL_URL")
}

model users {
  age       Int
  email     String @unique
  firstName String
  gametag   String
  id        Int    @id
  lastName  String
  password  String
}