type User {
  id: ID!
  email: ID!
  password: String!
}

type Query {
  users: [User]
  user(id: ID!): User
}

input NewUserInput {
  email: String!
  password: String!
}

type Mutation {
  newUser(input: NewUserInput!): User
}
