type Query {
  hello(name: String!): String!
  users: [User]
  user(id: ID!): User
}

type Subscription {
  hello(name: String!): String!
}

type User {
  id: ID!
  email: ID!
  password: String!
}

input NewUserInput {
  email: String!
  password: String!
}

type Mutation {
  newUser(input: NewUserInput!): User
}

schema {
  query: Query
  mutation: Mutation
  subscription: Subscription
}
