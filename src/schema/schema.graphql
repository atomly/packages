type Query {
  hello(name: String!): String!
  users: [User]
  user(id: ID!): User
}

type Subscription {
  hello(name: String!): String!
}

type User {
  id: ID!
  email: ID!
  password: String!
}

type Mutation {
  newUser(email: String!, password: String!): User
}

schema {
  query: Query
  mutation: Mutation
  subscription: Subscription
}
