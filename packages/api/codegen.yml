overwrite: true
schema: "./src/schema/schema.graphql"
documents: null
generates:
  src/types/schema.d.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"
